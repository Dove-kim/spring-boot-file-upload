spring:
  config:
    import:
      - classpath:credential.yml
  servlet:
    multipart:
      max-file-size: 1GB
      max-request-size: 2GB

cloud:
  aws:
    stack:
      auto: false
    region:
      static: ap-south-1

